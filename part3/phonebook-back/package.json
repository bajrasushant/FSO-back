{
  "name": "phonebook",
  "version": "1.0.0",
  "description": "returns phonebook entries",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd ../phonebook-front && npm run build && cp -r dist ../phonebook-back",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild"
  },
  "author": "Sushant Bajracharya",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
