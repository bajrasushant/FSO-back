{
  "name": "phonebook",
  "version": "1.0.0",
  "description": "returns phonebook entries",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd ../front-phonebook && npm run build && cp -r dist ../back-phonebook",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild"
  },
  "author": "Sushant Bajracharya",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mongodb": "^6.2.0",
    "mongoose": "^8.0.0",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
